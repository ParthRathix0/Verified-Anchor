[package]
name = "verified-anchor-program"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
solana-program = "2"
verified-anchor = { path = "../verified-anchor" }
